# Copy to .env.local — Vite uses VITE_ prefix for env vars

# Leave blank to proxy through vite.config.js to localhost:8080
VITE_API_URL=

# Or point directly at your backend:
# VITE_API_URL=http://localhost:8080/api
